<% if logged_in? %>
  <p>ログイン完了ユーザ: <%= current_user.name %></p>
<% else %>
<div class="center jumbotron">
  <div class="text-center">
    <h1>Welcom To TaskLisk</h1>
    <%= link_to 'Sign up now!', signup_path, class: 'btn btn-primary' %>
  </div>
</div>
<% end %>